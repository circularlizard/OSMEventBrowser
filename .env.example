# OSM OAuth 2.0 Configuration
# Get these values from https://www.onlinescoutmanager.co.uk/oauth/authorise

# Your OSM OAuth Client ID
OSM_CLIENT_ID=your_client_id_here

# Your OSM OAuth Client Secret (KEEP THIS SECRET!)
OSM_CLIENT_SECRET=your_client_secret_here

# OAuth Redirect URI (must match what you registered with OSM)
# For local development: https://localhost:3000/api/oauth-callback
# For production: https://your-app.vercel.app/api/oauth-callback
OSM_REDIRECT_URI=https://localhost:3000/api/oauth-callback

# OSM API Base URL
OSM_API_BASE_URL=https://www.onlinescoutmanager.co.uk

# Session secret for cookie encryption (generate a random string)
SESSION_SECRET=generate_a_random_secret_here_at_least_32_characters_long

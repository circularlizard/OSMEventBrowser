# Components (`src/components`)

This directory contains all React components used in the application.

## Structure

- **ui/**: Generic, reusable UI components (buttons, inputs, cards). These are primarily from **shadcn/ui**.
- **osm/**: Domain-specific components related to Online Scout Manager (e.g., Section Selectors, Term Selectors).
